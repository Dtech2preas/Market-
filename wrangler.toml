name = "dtech-student-hub-worker"
main = "worker.js"
compatibility_date = "2024-06-22"

# Replace this ID with your actual KV namespace ID from the Cloudflare Dashboard
[[kv_namespaces]]
binding = "MARKET_KV"
id = "YOUR_KV_NAMESPACE_ID_HERE"

[vars]
# IMGBB API key is passed dynamically via GitHub secrets, but defined here to show it exists
